using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public interface IDamagable {

	//This will only contain a death function as there is no HP in this game
	//The snake and the mouse can both die

	void Die();
}
